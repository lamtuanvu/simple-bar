const CustomStyles = /* css */ `
.simple-bar--process {
  background-color: rgba(255, 255, 255, 0.4);
  backdrop-filter: blur(2px);
}
.spotify,
.zoom,
.music,
.browser-track,
.battery:not(.battery--caffeinate),
.mic,
.sound,
.wifi,
.date,
.time,
.weather {
  background-color: #fff;
}
`

export default CustomStyles
